{
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "cypress run --quiet",
    "ci": "start-server-and-test preview http://localhost:3000 test"
  },
  "devDependencies": {
    "@astrojs/tailwind": "^0.2.4",
    "astro": "^1.0.0-beta.72",
    "cypress": "^10.3.0",
    "start-server-and-test": "^1.14.0"
  }
}
